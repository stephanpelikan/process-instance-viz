spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/test
        username: test
        password: test

camunda:
    bpm:
        admin-user:
            id: admin
            password: admin
